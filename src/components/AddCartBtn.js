import React from "react";

const AddCartBtn = ({}) => {
  return (
    <button className="bg-blue-500 hover:bg-blue-700 text-white font-bold text-xs px-4 rounded">
      Add to Cart
    </button>
  );
};

export default AddCartBtn;
